---
layout: lecture
pretty_table: true
order: 1
title: CD/CI with Docker Compose
mermaid:
  enabled: true
  zoomable: true
code_diff: true
map: true
chart:
  chartjs: true
  echarts: true
  vega_lite: true
tikzjax: true
typograms: true
last_updated: 2026-02-24

toc:
  - name: Set Up Environment
  - name: Prepare the Service
  - name: Good Things Does Not Last
---

## Set Up Environment 

{% details CloudLab %}

- Deploy your Cloud experiment, Docker branch. 
- For today's lecture, each student should launch their own experiment. 

{% enddetails %}

{% details Setup GitHub repository %}

- Visit [branch go_app_compose_cdci of the hello repository](https://github.com/CSC468-WCU/hello/tree/go_app_compose_cdci)
- Create a new repository, and **make sure** that you have copied all contents from the branch. 
    - *We will modify the contents later.* 

{% enddetails %}

{% details Docker Hub Token %}

- Open a browser, visit Docker Hub and login to your account. 
- On the top right corner, click on your profile, then select `Account Settings`
- Select `Personal access tokens`

{% include figure.liquid path="assets/img/courses/csc468/compose-cd-ci/hub-token-1.png" max-width="50%" zoomable=true %}

- Click on `Generate new token`
    - `Access token description`: GitHub Secret
    - `Expiration date`: 90 days
    - `Access permissions`: Read & Write
- Then click on `Generate`

{% include figure.liquid path="assets/img/courses/csc468/compose-cd-ci/hub-token-2.png" max-width="50%" zoomable=true %}

- Keep the page with the personal access token open, as we will need that for later. 

{% include figure.liquid path="assets/img/courses/csc468/compose-cd-ci/hub-token-3.png" max-width="50%" zoomable=true %}

{% enddetails %}
{% details Setup GitHub secret %}

- Go to the GitHub repository that you crealed earlier. 
- Visit `Settings`/`Secrets and variables`/`Actions`/`new repository secret`

{% include figure.liquid path="assets/img/courses/csc468/compose-cd-ci/hub-token-4.png" max-width="50%" zoomable=true %}

- Add the following secrets:
    - `DOCKERHUB_TOKEN`: your DockerHub login name
    - `DOCKERHUB_USERNAME`: The secret token generated by Docker Hub earlier. 

{% include figure.liquid path="assets/img/courses/csc468/compose-cd-ci/hub-token-5.png" max-width="50%" zoomable=true %}

{% enddetails %}
{% details Setup GitHub runner %}

- Go to the GitHub repository that you crealed earlier. 
- Visit `Settings`/`Actions`/`Runners`
- Click on `New self-hosted runner`
    - Select `Linux`/`x64`
    - SSH into your Docker experiment on CloudLab
    - Run all the commands under `Download` and `Configure` section. 
    - Keep the terminal open. 

{% enddetails %}
---

## Prepare The Service

{% details Making edit %}

- You can do this directly via the GitHub browser environment. 
- Modify `docker-compose.yml` as follows
    - Adjust lines 6 and 8 to replace my Docker Hub username with your DockerHub username.
- Modify `.github/workflows/publish.yml` as follows
    - Adjust lines 5 and 15 with the branch containing your contents (most likely `main`)
    - Adjust line 32 to replace my Docker Hub username with your DockerHub username.

{% enddetails %}
{% details Deploy your service %}

- Login into your CloudLab Docker experiment using another terminal. 
- Clone the Git repository (use HTTPS option)
- Change into the repository and run the followings:

```bash
docker compose build go_app --no-cache
docker compose up -d
```

- Open a browser tab and use your experiment hostname at port 8080. 

{% enddetails %}
{% details Observe CD/CI %}

- Change to the test being displayed by editing the `main.go` file. 
- Observe the Action workflow being carried out, and that the browser display the content 
of the web app being changed by refreshing. 
{% enddetails %}

## Good Things Does Not Last

- Community project `containerr/Towerwatch` is no longer supported as of this year. 
- Everything can be fully controll via GitHub Actions. 
